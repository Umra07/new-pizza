export { SortPropertyEnum, PizzaCategory, type Sort } from './model/types';
export {
  filtersSlice,
  filtersSliceSelector,
  setCategory,
  setSortType,
  setCurrentPage,
  setFilters,
} from './model/slice';
